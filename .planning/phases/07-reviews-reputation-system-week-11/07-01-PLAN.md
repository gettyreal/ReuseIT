---
phase: 07-reviews-reputation-system-week-11
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/migrations/20260506_phase07_reviews.sql
  - src/Repositories/ReviewRepository.php
  - src/ValueObjects/Rating.php
autonomous: true
requirements: [REV-02, REV-04, REV-05]

must_haves:
  truths:
    - "Reviews table exists with booking reference and immutability constraints"
    - "User profile denormalization supports average rating and review count display"
    - "Review queries include soft-delete filtering for deleted users"
    - "Rating value object enforces 1-5 whole-star range validation"
  artifacts:
    - path: "config/migrations/20260506_phase07_reviews.sql"
      provides: "Reviews schema, user denormalization columns, and indexes"
      contains: "reviews table, avg_rating, total_reviews columns"
    - path: "src/Repositories/ReviewRepository.php"
      provides: "Review persistence and retrieval with soft-delete filtering"
      exports: ["create", "find", "findByUserId", "findByBookingId", "countByUserId", "exists"]
    - path: "src/ValueObjects/Rating.php"
      provides: "Immutable 1-5 star validation"
      exports: ["Rating.__construct", "getValue", "getAsStars"]
  key_links:
    - from: "src/Repositories/ReviewRepository.php"
      to: "reviews"
      via: "CRUD operations with prepared statements"
      pattern: "INSERT INTO reviews|SELECT.*reviews"
    - from: "src/ValueObjects/Rating.php"
      to: "reviews.rating"
      via: "Domain validation before persistence"
      pattern: "Rating::.*|getValue"
    - from: "config/migrations/20260506_phase07_reviews.sql"
      to: "users table"
      via: "ADD avg_rating, total_reviews columns"
      pattern: "ALTER TABLE users|ADD COLUMN avg_rating|ADD COLUMN total_reviews"

---

<objective>
Create the persistent foundation for reviews: schema migration with user denormalization + ReviewRepository for data access + Rating value object for validation.

Purpose: Ensure review structure (immutability, booking reference, soft-delete) and denormalization (avg_rating, total_reviews) are in place before service logic.
Output: Migration SQL + ReviewRepository + Rating value object supporting review creation, read, and user statistics queries.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reviews-reputation-system-week-11/07-CONTEXT.md
@src/Repositories/BaseRepository.php
@src/ValueObjects/Price.php
@config/ReuseIT.sql
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Phase 7 reviews migration with user denormalization and immutability constraints</name>
  <files>config/migrations/20260506_phase07_reviews.sql</files>
  <action>Create an idempotent SQL migration (safe to run once in existing environments) that:
1) Create `reviews` table with:
   - `id` INT AUTO_INCREMENT PRIMARY KEY
   - `reviewer_user_id` INT NOT NULL FOREIGN KEY to users(id)
   - `reviewed_user_id` INT NOT NULL FOREIGN KEY to users(id)
   - `booking_id` INT NOT NULL FOREIGN KEY to bookings(id) UNIQUE (only one review per booking)
   - `rating` TINYINT NOT NULL CHECK (rating >= 1 AND rating <= 5) (whole stars only)
   - `comment` VARCHAR(500) NULL (optional, max 500 chars)
   - `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
   - `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
   - `deleted_at` TIMESTAMP NULL (soft delete support)
   - Indexes: (reviewed_user_id, created_at) for profile history, (booking_id) for duplicate prevention, (reviewer_user_id, created_at) for user's posted reviews

2) Alter `users` table to add denormalization columns:
   - `avg_rating` DECIMAL(3,2) NULL DEFAULT NULL (e.g., 4.50)
   - `total_reviews` INT DEFAULT 0 (count of non-deleted reviews received)
   - Indexes: (avg_rating) for "top-rated" queries, none needed for total_reviews

3) Add NOT NULL constraints as appropriate and verify soft-delete filtering will work via Softdeletable trait.
Do not modify other tables; keep DDL migration-only.</action>
  <verify>
    <automated>rg -n "CREATE TABLE reviews|reviewer_user_id|reviewed_user_id|booking_id|UNIQUE|CHECK.*rating|avg_rating|total_reviews|deleted_at" config/migrations/20260506_phase07_reviews.sql</automated>
    <manual>Confirm migration includes immutability via UNIQUE(booking_id) and review.comment has 500-char limit.</manual>
  </verify>
  <done>Reviews table exists with booking uniqueness constraint, user denormalization columns added, soft-delete column present, all indexes in place.</done>
</task>

<task type="auto">
  <name>task 2: Implement ReviewRepository with CRUD and user statistics queries</name>
  <files>src/Repositories/ReviewRepository.php</files>
  <action>Create `ReviewRepository` extending `BaseRepository` with methods:
- `create(array $data): int` — Insert review (reviewer_id, reviewed_user_id, booking_id, rating, comment). Return review id.
- `find(int $id): ?array` — Get single review by id (soft-delete filtered).
- `findByBookingId(int $bookingId): ?array` — Get review for specific booking (prevent duplicates).
- `findByUserId(int $userId, int $limit, int $offset): array` — Get all reviews received by userId, newest-first, soft-delete filtered. Returns array of review records with reviewer info (select joined with users for reviewer name/avatar).
- `countByUserId(int $userId): int` — Count total non-deleted reviews received by user.
- `exists(int $bookingId, int $reviewerId): bool` — Check if review already exists for booking (rate limit prevention).
Include soft-delete filtering (deleted_at IS NULL) on all SELECT queries via Softdeletable trait or explicit clauses. Use prepared statements throughout.
Key: ReviewRepository must NOT update avg_rating or total_reviews; that responsibility goes to ReviewService in Wave 2.</action>
  <verify>
    <automated>php -l src/Repositories/ReviewRepository.php && rg -n "create|findByBookingId|findByUserId|countByUserId|exists|prepare\(|deleted_at IS NULL" src/Repositories/ReviewRepository.php</automated>
    <manual>Verify findByUserId joins users table for reviewer metadata and orders by created_at DESC (newest first).</manual>
  </verify>
  <done>ReviewRepository supports read/write operations with soft-delete filtering and user statistics queries needed for profile display.</done>
</task>

<task type="auto">
  <name>task 3: Implement Rating value object for 1-5 star validation</name>
  <files>src/ValueObjects/Rating.php</files>
  <action>Create `Rating` value object (immutable) with:
- Constructor: `__construct(int $value)` — Validate $value is 1-5 inclusive. Throw InvalidArgumentException if outside range or not integer.
- Public methods:
  - `getValue(): int` — Return the numeric rating (1-5).
  - `getAsStars(): string` — Return visual star representation (e.g., "★★★★☆" for 4 stars). Use ★ for filled, ☆ for empty.
  - `equals(Rating $other): bool` — Compare ratings.
  - `__toString(): string` — Return numeric string "1" through "5".
- No setters; immutable after construction.
- Use same pattern as existing Price, Category, BookingStatus value objects.
This object is used by ReviewService to validate input before persistence.</action>
  <verify>
    <automated>php -l src/ValueObjects/Rating.php && rg -n "class Rating|__construct|getValue|getAsStars|InvalidArgumentException|private.*value" src/ValueObjects/Rating.php</automated>
    <manual>Test Rating(4)->getAsStars() returns "★★★★☆" and Rating(0) throws InvalidArgumentException.</manual>
  </verify>
  <done>Rating value object enforces 1-5 range and provides star visualization for frontend and persistence.</done>
</task>

</tasks>

<verification>
Run syntax checks on migration and repository; verify Rating value object instantiation.
</verification>

<success_criteria>
- Reviews table exists with UNIQUE(booking_id) constraint and deleted_at soft-delete column.
- User denormalization columns (avg_rating, total_reviews) added to users table.
- ReviewRepository supports create, read, and user-statistics queries with soft-delete filtering.
- Rating value object enforces 1-5 whole-star validation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-reviews-reputation-system-week-11/07-01-SUMMARY.md`
</output>

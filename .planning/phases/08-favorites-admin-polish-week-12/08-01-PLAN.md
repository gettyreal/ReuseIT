---
phase: 08-favorites-admin-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/Models/Favorite.php, src/Repositories/FavoriteRepository.php, src/Models/Report.php, src/Repositories/ReportRepository.php, src/Migrations/CreateFavoritesAndReportsTable.php, config/Database.php, src/Models/User.php]
autonomous: true
requirements: [FAV-01, FAV-02, FAV-03, ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "favorites table exists with (id, user_id, listing_id, created_at, deleted_at)"
    - "reports table exists with (id, reporter_id, reported_type, reported_id, reason, status, comment, created_at, deleted_at)"
    - "users table has is_admin boolean column defaulting to false"
    - "FavoriteRepository can create, find, delete favorite records"
    - "ReportRepository can create, find, list, update report status"
    - "Soft-delete filtering applied to all SELECT queries"
  artifacts:
    - path: "src/Models/Favorite.php"
      provides: "Domain model for favorite relationships"
    - path: "src/Models/Report.php"
      provides: "Domain model for user reports"
    - path: "src/Repositories/FavoriteRepository.php"
      provides: "Data access for favorites (CRUD operations)"
    - path: "src/Repositories/ReportRepository.php"
      provides: "Data access for reports (CRUD + status tracking)"
    - path: "src/Migrations/CreateFavoritesAndReportsTable.php"
      provides: "Schema definitions for favorites and reports"
  key_links:
    - from: "FavoriteRepository"
      to: "favorites table"
      via: "prepared statement queries"
      pattern: "INSERT|SELECT|DELETE.*favorites"
    - from: "ReportRepository"
      to: "reports table"
      via: "prepared statement queries with soft-delete filtering"
      pattern: "WHERE.*deleted_at IS NULL"
    - from: "User model"
      to: "is_admin column"
      via: "role detection"
      pattern: "is_admin.*=.*true|false"
---

<objective>
Create data layer foundations for Phase 8: favorites system and admin reporting infrastructure. Establishes database schema, models, and repositories that enable subsequent service and controller layers.

Purpose: Favorites and reports require durable storage with consistent access patterns. Foundational models establish immutable domain contracts; repositories implement CRUD operations with soft-delete filtering.

Output: Database schema migration, 4 repository/model files, admin role field on users table
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Established patterns from Phase 1-7
- BaseRepository with soft-delete filtering via Softdeletable trait
- PDO prepared statements (0 SQL injection vulnerabilities)
- Value Objects for immutable domain types
- Soft delete strategy: deleted_at IS NULL filtering in all SELECT queries
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Favorites schema migration and FavoriteRepository</name>
  <files>src/Migrations/CreateFavoritesAndReportsTable.php, src/Models/Favorite.php, src/Repositories/FavoriteRepository.php</files>
  <action>
1. Create favorites table migration (src/Migrations/CreateFavoritesAndReportsTable.php):
   - Table: favorites
   - Columns: id (UUID primary key), user_id (FK users), listing_id (FK listings), created_at, updated_at, deleted_at
   - Constraints: UNIQUE(user_id, listing_id, deleted_at IS NULL) to prevent duplicate favorites
   - Soft delete: Include deleted_at column with default NULL
   - Index on (user_id, created_at DESC) for efficient pagination

2. Create Favorite.php immutable value object:
   - Properties: id, user_id, listing_id, created_at
   - Type-safe read-only access

3. Create FavoriteRepository.php extending BaseRepository:
   - findByUserId(user_id, limit=20, offset=0): Return paginated favorites with user's favorites in created_at DESC order
   - findByUserAndListing(user_id, listing_id): Check if favorite exists (for toggle logic)
   - create(user_id, listing_id): INSERT new favorite, return Favorite object. Handle UNIQUE constraint violation gracefully (duplicate favorite = idempotent no-op, not error)
   - delete(favorite_id): Soft-delete by setting deleted_at = NOW()
   - countByUser(user_id): Total active favorites for user (for UI badge)
   - Soft-delete filtering: All SELECT queries must include "WHERE deleted_at IS NULL" via Softdeletable trait

Security & Validation:
   - All queries use prepared statements with ? placeholders
   - user_id and listing_id are validated as non-empty UUIDs before repository calls
   - Authorization (can only favorite own favorites) enforced in service layer, not repository
  </action>
  <verify>
    <automated>
      php -r "
        require 'vendor/autoload.php';
        require 'config/Database.php';
        
        // Verify favorites table exists with correct columns
        \$db = Database::getInstance();
        \$result = \$db->query('DESCRIBE favorites');
        \$columns = \$result->fetchAll(PDO::FETCH_COLUMN, 0);
        
        \$required = ['id', 'user_id', 'listing_id', 'created_at', 'updated_at', 'deleted_at'];
        \$missing = array_diff(\$required, \$columns);
        
        if (!empty(\$missing)) {
          echo 'FAIL: Missing columns: ' . implode(', ', \$missing);
          exit(1);
        }
        
        // Verify UNIQUE constraint exists
        \$constraints = \$db->query('SELECT CONSTRAINT_NAME FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE WHERE TABLE_NAME=\"favorites\" AND COLUMN_NAME=\"user_id\"')->fetchAll();
        if (empty(\$constraints)) {
          echo 'FAIL: No unique constraint on user_id';
          exit(1);
        }
        
        // Verify FavoriteRepository class exists and extends BaseRepository
        if (!class_exists('App\\Repositories\\FavoriteRepository')) {
          echo 'FAIL: FavoriteRepository class not found';
          exit(1);
        }
        
        \$ref = new ReflectionClass('App\\Repositories\\FavoriteRepository');
        if (\$ref->getParentClass()->getName() !== 'App\\Repositories\\BaseRepository') {
          echo 'FAIL: FavoriteRepository does not extend BaseRepository';
          exit(1);
        }
        
        // Verify required methods exist
        \$methods = ['findByUserId', 'findByUserAndListing', 'create', 'delete', 'countByUser'];
        foreach (\$methods as \$method) {
          if (!method_exists('App\\Repositories\\FavoriteRepository', \$method)) {
            echo \"FAIL: Missing method: \$method\";
            exit(1);
          }
        }
        
        echo 'PASS: Favorites table, constraints, FavoriteRepository, and all methods verified';
      "
    </automated>
    <sampling_rate>run after task commits, before task 2 begins</sampling_rate>
  </verify>
  <done>
    - Favorites table created with UNIQUE constraint on (user_id, listing_id)
    - FavoriteRepository class exists with 5 required methods
    - All queries use prepared statements
    - Soft-delete filtering applied to all SELECT queries
    - Created/updated/deleted timestamp columns present
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Reports schema migration and ReportRepository</name>
  <files>src/Models/Report.php, src/Repositories/ReportRepository.php</files>
  <action>
1. Add reports table to migration (append to CreateFavoritesAndReportsTable.php if not already present):
   - Table: reports
   - Columns: id (UUID), reporter_id (FK users), reported_type (ENUM: 'listing', 'user'), reported_id (VARCHAR 36 for UUID), reason (ENUM: 'spam', 'fake', 'illegal', 'harmful'), status (ENUM: 'pending', 'approved', 'rejected' DEFAULT 'pending'), comment (TEXT optional), created_at, updated_at, deleted_at
   - Constraints: FOREIGN KEY reporter_id → users(id), CHECK reason IN ('spam','fake','illegal','harmful'), CHECK reported_type IN ('listing','user'), CHECK status IN ('pending','approved','rejected')
   - Soft delete: Include deleted_at column
   - Indexes: (status, created_at DESC) for admin queue, (reported_type, reported_id) for content retrieval

2. Create Report.php immutable value object:
   - Properties: id, reporter_id, reported_type, reported_id, reason, status, comment, created_at
   - Enums/constants for reason (SPAM, FAKE, ILLEGAL, HARMFUL) and status (PENDING, APPROVED, REJECTED)
   - Type-safe read-only access

3. Create ReportRepository.php extending BaseRepository:
   - create(reporter_id, reported_type, reported_id, reason, comment): Create new report, return Report. Validate reporter_id != reported_id if reported_type='user' (can't report self)
   - findByStatus(status, limit=20, offset=0): Return paginated reports ordered by created_at DESC. Used by admin queue.
   - findByReportedContent(reported_type, reported_id): List all reports for a specific listing/user
   - findById(report_id): Single report retrieval
   - updateStatus(report_id, new_status): PATCH to set status to approved/rejected (used by admin)
   - countByStatus(status): Count pending/approved/rejected reports (for admin stats)
   - Soft-delete filtering: All SELECT queries must include "WHERE deleted_at IS NULL"

Security & Validation:
   - All queries use prepared statements
   - reported_type must be 'listing' or 'user'
   - reason must be one of enum values
   - reporter_id validation: must be valid UUID and authenticated user
   - Authorization: Only reporters can see their own reports; admins see all
  </action>
  <verify>
    <automated>
      php -r "
        require 'vendor/autoload.php';
        require 'config/Database.php';
        
        // Verify reports table exists with correct columns
        \$db = Database::getInstance();
        \$result = \$db->query('DESCRIBE reports');
        \$columns = \$result->fetchAll(PDO::FETCH_COLUMN, 0);
        
        \$required = ['id', 'reporter_id', 'reported_type', 'reported_id', 'reason', 'status', 'comment', 'created_at', 'updated_at', 'deleted_at'];
        \$missing = array_diff(\$required, \$columns);
        
        if (!empty(\$missing)) {
          echo 'FAIL: Missing columns: ' . implode(', ', \$missing);
          exit(1);
        }
        
        // Verify ReportRepository class exists
        if (!class_exists('App\\Repositories\\ReportRepository')) {
          echo 'FAIL: ReportRepository class not found';
          exit(1);
        }
        
        // Verify required methods exist
        \$methods = ['create', 'findByStatus', 'findByReportedContent', 'findById', 'updateStatus', 'countByStatus'];
        foreach (\$methods as \$method) {
          if (!method_exists('App\\Repositories\\ReportRepository', \$method)) {
            echo \"FAIL: Missing method: \$method\";
            exit(1);
          }
        }
        
        echo 'PASS: Reports table and ReportRepository verified';
      "
    </automated>
    <sampling_rate>run after task commits, before task 3 begins</sampling_rate>
  </verify>
  <done>
    - Reports table created with enums for reason (spam, fake, illegal, harmful) and status (pending, approved, rejected)
    - ReportRepository class exists with 6 required methods
    - All queries use prepared statements with parameter binding
    - Soft-delete filtering applied to all SELECT queries
    - FOREIGN KEY constraint on reporter_id
  </done>
</task>

<task type="auto">
  <name>Task 3: Add admin role field to users table and update User model</name>
  <files>config/Database.php, src/Models/User.php</files>
  <action>
1. Alter users table (add to migration or direct ALTER):
   - ADD COLUMN is_admin BOOLEAN DEFAULT FALSE
   - Index on is_admin for efficient admin user queries
   - Rationale: Enables role-based access control; false by default (security principle of least privilege)

2. Update User.php model:
   - Add is_admin property (boolean)
   - Add getIsAdmin() or isAdmin() method for role checking
   - Ensure read-only field (set during instantiation, not modifiable post-creation)

3. Update any existing User instantiation in UserRepository:
   - Ensure is_admin field is populated from database in find() and findByEmail() methods
   - Verify User::find() includes is_admin in SELECT clause

Security & Authorization:
   - Admin role checked in admin controller middleware (defer to Plan 04)
   - is_admin stored in database, not JWT/session (prevents elevation via session tampering)
   - Default false ensures only explicitly promoted users can admin
  </action>
  <verify>
    <automated>
      php -r "
        require 'vendor/autoload.php';
        require 'config/Database.php';
        
        \$db = Database::getInstance();
        
        // Verify is_admin column exists
        \$result = \$db->query('DESCRIBE users');
        \$columns = \$result->fetchAll(PDO::FETCH_COLUMN, 0);
        
        if (!in_array('is_admin', \$columns)) {
          echo 'FAIL: is_admin column not found in users table';
          exit(1);
        }
        
        // Verify User model has isAdmin() method
        if (!method_exists('App\\Models\\User', 'isAdmin')) {
          echo 'FAIL: User model missing isAdmin() method';
          exit(1);
        }
        
        // Test: Create test user, verify is_admin defaults to false
        // (Deferred to service layer tests in Plan 02)
        
        echo 'PASS: is_admin column added, User model updated';
      "
    </automated>
    <sampling_rate>run after task commits, before proceeding to Plan 02</sampling_rate>
  </verify>
  <done>
    - is_admin column added to users table with DEFAULT FALSE
    - User model updated with isAdmin() method
    - UserRepository findById/findByEmail includes is_admin in SELECT
    - Admin role field properly initialized on user instantiation
  </done>
</task>

</tasks>

<verification>
Database migrations executed successfully:
- `php config/Database.php` runs all pending migrations
- Verify `DESCRIBE favorites` and `DESCRIBE reports` show correct columns
- Verify UNIQUE constraint exists on (user_id, listing_id) in favorites
- Verify FOREIGN KEY constraint exists on reporter_id in reports
- All SELECT queries in repositories include soft-delete filtering (WHERE deleted_at IS NULL)
</verification>

<success_criteria>
- [ ] Favorites table exists with id, user_id, listing_id, created_at, updated_at, deleted_at, and UNIQUE(user_id, listing_id, deleted_at IS NULL)
- [ ] Reports table exists with id, reporter_id, reported_type, reported_id, reason, status, comment, created_at, updated_at, deleted_at
- [ ] users.is_admin column exists, defaults to FALSE
- [ ] FavoriteRepository has 5 methods: findByUserId, findByUserAndListing, create, delete, countByUser
- [ ] ReportRepository has 6 methods: create, findByStatus, findByReportedContent, findById, updateStatus, countByStatus
- [ ] All repository queries use prepared statements (0 SQL injection patterns)
- [ ] Soft-delete filtering applied in all SELECT queries
- [ ] Both repositories extend BaseRepository correctly

</success_criteria>

<output>
After completion, create `.planning/phases/08-favorites-admin-polish-week-12/08-01-SUMMARY.md` containing:
- List of tables created: favorites, reports
- List of classes created: Favorite, Report, FavoriteRepository, ReportRepository
- List of columns added: users.is_admin
- Verification: All tests passed, all queries use prepared statements
- Ready for: Plan 02 (Services & Business Logic)
</output>

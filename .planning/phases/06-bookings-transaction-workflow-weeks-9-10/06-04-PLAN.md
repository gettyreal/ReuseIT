---
phase: 06-bookings-transaction-workflow-weeks-9-10
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/Controllers/BookingController.php
  - src/Repositories/PickupWindowRepository.php
autonomous: true
gap_closure: true
requirements: [BOOK-01, BOOK-02, BOOK-03, BOOK-04, BOOK-05, BOOK-06, BOOK-07, BOOK-08, CHAT-06]

must_haves:
  truths:
    - "Only next valid actions are exposed per role/status for confirmed bookings"
    - "Seller cannot counter pickup before buyer proposes a window"
    - "Non-proposing participant can accept an open pickup proposal"
    - "Action hints remain aligned with service transition guards and buyer-first scheduling policy"
  artifacts:
    - path: "src/Controllers/BookingController.php"
      provides: "Role-aware next_actions derived from pickup negotiation state"
      contains: "computeNextActions"
    - path: "src/Repositories/PickupWindowRepository.php"
      provides: "Open pickup proposal lookup for dashboard action gating"
      exports: ["findLatestOpenByBookingId"]
  key_links:
    - from: "src/Controllers/BookingController.php"
      to: "src/Repositories/PickupWindowRepository.php"
      via: "lookup of latest open pickup proposal per booking"
      pattern: "findLatestOpenByBookingId"
    - from: "src/Controllers/BookingController.php"
      to: "next_actions payload"
      via: "role + booking_status + proposal proposer logic"
      pattern: "accept_pickup|counter_pickup|propose_pickup"
---

<objective>
Close the Phase 6 verification gap by making booking dashboard action hints state-complete and role-accurate for pickup negotiation states.

Purpose: Ensure API `next_actions` never suggests invalid workflow actions and always surfaces valid actions that the current actor can take.
Output: Updated pickup-proposal lookup + corrected `next_actions` derivation in booking payloads.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-CONTEXT.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-RESEARCH.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-VERIFICATION.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-03-SUMMARY.md
@src/Controllers/BookingController.php
@src/Repositories/PickupWindowRepository.php
</context>

<tasks>

<task type="auto">
  <name>task 1: Add repository helper for latest open pickup proposal</name>
  <files>src/Repositories/PickupWindowRepository.php</files>
  <action>Add a method `findLatestOpenByBookingId(int $bookingId): ?array` that returns the most recent non-deleted pickup proposal with `proposal_status IN ('proposed','countered')`, ordered newest-first. Keep prepared statement style consistent with existing repository methods. Do not alter accepted-window behavior in `findLatestAcceptedByBookingId`.</action>
  <verify>
    <automated>php -l src/Repositories/PickupWindowRepository.php && rg -n "findLatestOpenByBookingId|proposal_status IN \('proposed', 'countered'\)" src/Repositories/PickupWindowRepository.php</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Controller can query authoritative open proposal state without scanning all pickup rows.</done>
</task>

<task type="auto">
  <name>task 2: Recompute next_actions from role + latest open proposal state</name>
  <files>src/Controllers/BookingController.php</files>
  <action>Update dashboard enrichment logic so `next_actions` for `booking_status=confirmed` is derived from: actor role (buyer/seller), latest open proposal presence, and proposer identity.

Required behavior:
- If no open proposal exists: buyer sees `propose_pickup`, seller does not see `counter_pickup` (buyer-first rule).
- If open proposal exists: non-proposer sees `accept_pickup` and `counter_pickup`; proposer must not see `accept_pickup` on their own proposal.
- Keep `cancel` visible for both participants in confirmed state.
- Keep `complete` visible only when an accepted pickup window exists.

Implementation notes:
- Fetch latest open proposal per booking (using new repository helper) during bucket enrichment and pass it to action computation.
- Preserve existing pending-state actions and seller pending countdown metadata.
- Keep response field names unchanged (`next_actions`, `respond_by`, etc.) for backward compatibility.</action>
  <verify>
    <automated>php -l src/Controllers/BookingController.php && rg -n "accept_pickup|counter_pickup|propose_pickup|findLatestOpenByBookingId|next_actions" src/Controllers/BookingController.php</automated>
    <manual>Run booking flow states (confirmed/no proposal, buyer proposal open, seller counter open, accepted window) and verify payload shows only valid actions per actor and includes `accept_pickup` for non-proposer on open proposals.</manual>
    <sampling_rate>run after this task commits, before phase summary</sampling_rate>
  </verify>
  <done>Verification truth 15 passes: API action hints match real workflow state and no longer expose premature/missing pickup actions.</done>
</task>

</tasks>

<verification>
`php -l src/Repositories/PickupWindowRepository.php && php -l src/Controllers/BookingController.php` passes and grep confirms open-proposal-aware action derivation (`accept_pickup`, gated `counter_pickup`, buyer-first `propose_pickup`).
</verification>

<success_criteria>
- `next_actions` no longer shows `counter_pickup` for seller when no pickup proposal exists.
- `next_actions` includes `accept_pickup` for the non-proposer when an open proposal exists.
- `next_actions` remains role/status-valid for pending and confirmed buckets while preserving current payload contract.
</success_criteria>

<output>
After completion, create `.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-04-SUMMARY.md`
</output>

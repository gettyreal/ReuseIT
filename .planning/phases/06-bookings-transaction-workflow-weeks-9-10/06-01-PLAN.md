---
phase: 06-bookings-transaction-workflow-weeks-9-10
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/migrations/20260330_phase06_bookings.sql
  - src/Repositories/BookingRepository.php
  - src/Repositories/BookingEventRepository.php
  - src/Repositories/PickupWindowRepository.php
autonomous: true
requirements: [BOOK-03, BOOK-04, BOOK-08]

must_haves:
  truths:
    - "Buyer and seller can retrieve bookings in separate role-specific views"
    - "Bookings can be ordered by urgency (deadline first) and then newest"
    - "Booking timeline events exist for state transitions and cancellation reasons"
    - "Active booking exclusivity can be enforced at DB level"
  artifacts:
    - path: "config/migrations/20260330_phase06_bookings.sql"
      provides: "Bookings schema upgrades, constraints, and supporting tables"
      contains: "booking_events"
    - path: "src/Repositories/BookingRepository.php"
      provides: "Booking reads/writes, locking queries, dashboard list queries"
      exports: ["find", "findForUpdate", "findActiveByListingForUpdate", "findByRoleAndStatus", "create", "update"]
    - path: "src/Repositories/BookingEventRepository.php"
      provides: "Booking timeline event persistence and retrieval"
      exports: ["create", "findByBookingId"]
    - path: "src/Repositories/PickupWindowRepository.php"
      provides: "Pickup proposal/counter/accept persistence"
      exports: ["create", "findLatestAcceptedByBookingId", "findByBookingId"]
  key_links:
    - from: "src/Repositories/BookingRepository.php"
      to: "bookings"
      via: "FOR UPDATE locking + active booking checks"
      pattern: "FOR UPDATE"
    - from: "src/Repositories/BookingEventRepository.php"
      to: "booking_events"
      via: "append-only timeline writes"
      pattern: "INSERT INTO booking_events"
    - from: "src/Repositories/PickupWindowRepository.php"
      to: "booking_pickup_windows"
      via: "proposal and acceptance history"
      pattern: "booking_pickup_windows"
---

<objective>
Create the persistent foundation for bookings: schema migration + repositories for booking state, timeline visibility, and pickup-window history.

Purpose: Ensure Phase 6 rules (status machine, cancellation auditability, urgency ordering, role views) are enforceable and queryable before service/controller logic.
Output: Migration SQL + 3 repositories supporting booking writes, timeline events, pickup windows, and dashboard reads.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-CONTEXT.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-RESEARCH.md
@src/Repositories/BaseRepository.php
@src/Repositories/ConversationRepository.php
@config/ReuseIT.sql
</context>

<tasks>

<task type="auto">
  <name>task 1: Add Phase 6 booking migration with constraints and timeline tables</name>
  <files>config/migrations/20260330_phase06_bookings.sql</files>
  <action>Create an idempotent SQL migration (safe to run once in existing environments) that adds/updates booking workflow schema:
1) `bookings` upgrades: `expires_at`, `cancelled_at`, `cancelled_by_user_id`, `cancel_reason_code`, `cancel_reason_note`, and strict status CHECK for `pending|confirmed|completed|cancelled`.
2) Add generated/stored column for active-booking exclusivity and unique index so only one active booking exists per listing (`pending`/`confirmed`) to prevent races.
3) Add `booking_pickup_windows` table for buyer proposal / seller counter / acceptance history with window start/end timestamps and proposal status.
4) Add `booking_events` table for timeline/audit events visible to both participants (`created`, `confirmed`, `pickup_proposed`, `pickup_countered`, `pickup_accepted`, `cancelled`, `expired`, `completed`), including actor, reason code, note, and event timestamp.
5) Add indexes supporting dashboard urgency ordering (`booking_status`, `expires_at`, `updated_at`, `buyer_id`, `seller_id`) and timeline reads (`booking_id`, `event_at`).
Do not modify unrelated legacy tables in this migration; keep runtime transactions DML-only (DDL only in migration).</action>
  <verify>
    <automated>rg -n "ALTER TABLE bookings|booking_pickup_windows|booking_events|expires_at|cancel_reason_code|UNIQUE KEY|CHECK" config/migrations/20260330_phase06_bookings.sql</automated>
    <manual>Confirm migration includes DB-level guard for active-booking exclusivity and schema support for cancellation reason + timeline visibility.</manual>
  </verify>
  <done>Migration file exists and explicitly models expiry, cancellation metadata, pickup-window history, timeline events, and active-booking exclusivity.</done>
</task>

<task type="auto">
  <name>task 2: Implement BookingRepository with locking queries and role dashboard reads</name>
  <files>src/Repositories/BookingRepository.php</files>
  <action>Create `BookingRepository` extending `BaseRepository` with methods used by BookingService and dashboards:
- `find(int $id): ?array` and `findForUpdate(int $id): ?array`.
- `findActiveByListingForUpdate(int $listingId): ?array` using `FOR UPDATE`.
- `findByRoleAndStatus(int $userId, string $role, ?string $status, int $limit, int $offset): array` with role split (`buyer` vs `seller`), status buckets, urgency-first sorting (pending: `expires_at ASC`, tie-break `created_at DESC`; others newest-first).
- `create(array $data): int`, `update(int $id, array $data): bool`.
- helper query for participant validation and listing ownership checks used by service.
Include soft-delete filtering where applicable and keep prepared statements throughout. Using urgency ordering and role separation per locked user decisions.</action>
  <verify>
    <automated>php -l src/Repositories/BookingRepository.php && rg -n "FOR UPDATE|findByRoleAndStatus|expires_at|booking_status|prepare\(" src/Repositories/BookingRepository.php</automated>
    <manual>Confirm seller and buyer query paths are separate and default ordering follows urgency-first then newest.</manual>
  </verify>
  <done>BookingRepository supports transactional locking, role-separated booking listing, status filtering, and urgency ordering with prepared SQL only.</done>
</task>

<task type="auto">
  <name>task 3: Implement booking timeline and pickup-window repositories</name>
  <files>src/Repositories/BookingEventRepository.php, src/Repositories/PickupWindowRepository.php</files>
  <action>Create two repositories:
1) `BookingEventRepository` for append-only event writes and chronological reads (`create`, `findByBookingId`).
2) `PickupWindowRepository` for proposal/counter/accept data (`create`, `findByBookingId`, `findLatestAcceptedByBookingId`, and helper to invalidate superseded pending proposals).
Store immutable history so cancellation/expiry/scheduling actions are visible to both sides. Keep event and pickup data independent from controller formatting.</action>
  <verify>
    <automated>php -l src/Repositories/BookingEventRepository.php && php -l src/Repositories/PickupWindowRepository.php && rg -n "INSERT INTO booking_events|booking_pickup_windows|findLatestAcceptedByBookingId" src/Repositories/BookingEventRepository.php src/Repositories/PickupWindowRepository.php</automated>
    <manual>Validate timeline reads are ordered chronologically and pickup repository can identify latest mutually accepted window.</manual>
  </verify>
  <done>Timeline and pickup-window repositories exist with methods required for cancellation transparency and BOOK-06 scheduling lifecycle.</done>
</task>

</tasks>

<verification>
Run repository syntax checks and ensure migration includes required Phase 6 structures.
</verification>

<success_criteria>
- Booking schema migration includes expiry, cancellation metadata, pickup-window history, and timeline table.
- BookingRepository supports FOR UPDATE locking and role-separated urgency-sorted reads.
- BookingEventRepository and PickupWindowRepository expose methods needed by BookingService transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-01-SUMMARY.md`
</output>

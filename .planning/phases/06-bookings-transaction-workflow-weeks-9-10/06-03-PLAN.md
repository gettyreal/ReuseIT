---
phase: 06-bookings-transaction-workflow-weeks-9-10
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/Controllers/BookingController.php
  - src/Router.php
  - src/Controllers/ChatController.php
autonomous: true
requirements: [BOOK-03, BOOK-05, BOOK-06, BOOK-07, BOOK-08]

must_haves:
  truths:
    - "User can create and inspect bookings via REST endpoints"
    - "Seller and buyer dashboards show role-specific grouped booking views"
    - "Seller sees pending bookings with explicit respond-by deadline and countdown metadata"
    - "Only next valid actions are exposed per role/status"
    - "Users can execute confirm/schedule/complete/cancel actions through API"
  artifacts:
    - path: "src/Controllers/BookingController.php"
      provides: "HTTP booking API endpoints + response shaping for role dashboards"
      exports: ["create", "list", "show", "confirm", "complete", "cancel", "proposePickup", "counterPickup", "acceptPickup"]
    - path: "src/Router.php"
      provides: "Protected booking route registrations + dependency injection"
      contains: "/api/bookings"
    - path: "src/Controllers/ChatController.php"
      provides: "Optional booking-scoped chat metadata enrichment"
      contains: "booking_id"
  key_links:
    - from: "src/Router.php"
      to: "src/Controllers/BookingController.php"
      via: "route mapping + dependency injection"
      pattern: "BookingController"
    - from: "src/Controllers/BookingController.php"
      to: "src/Services/BookingService.php"
      via: "all booking transitions delegated to service"
      pattern: "\$this->bookingService"
    - from: "src/Controllers/BookingController.php"
      to: "Response::success"
      via: "grouped dashboard payload + next actions"
      pattern: "next_actions|respond_by"
---

<objective>
Expose booking workflow through authenticated REST endpoints and shape role-specific dashboard responses with urgency/deadline semantics.

Purpose: Deliver user-visible transaction workflow for buyers/sellers while keeping service-layer rules authoritative.
Output: BookingController, Router booking routes, and optional chat response enrichment for booking context.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-CONTEXT.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-RESEARCH.md
@.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-02-SUMMARY.md
@src/Controllers/ChatController.php
@src/Controllers/ListingController.php
@src/Router.php
</context>

<tasks>

<task type="auto">
  <name>task 1: Create BookingController with CRUD-style and transition endpoints</name>
  <files>src/Controllers/BookingController.php</files>
  <action>Create `BookingController` using existing controller conventions (`$_SESSION['user_id']`, `Response::success/error/validationErrors`) and delegate all business rules to BookingService.
Implement endpoints:
- `POST /api/bookings` -> `create` (listing_id required)
- `GET /api/bookings` -> `list` (role=buyer|seller default: seller shows incoming pending bucket first, buyer shows my reservations)
- `GET /api/bookings/:id` -> `show`
- `PATCH /api/bookings/:id/confirm` -> `confirm` (seller only)
- `PATCH /api/bookings/:id/complete` -> `complete` (participant)
- `PATCH /api/bookings/:id/cancel` -> `cancel` (participant, reason_code required, note optional)
- `POST /api/bookings/:id/pickup/propose` -> `proposePickup`
- `POST /api/bookings/:id/pickup/counter` -> `counterPickup`
- `PATCH /api/bookings/:id/pickup/accept` -> `acceptPickup`
Map exceptions to appropriate HTTP codes (400 validation, 401 unauthenticated, 403 unauthorized, 404 not found, 409 state conflict).
Include timeline events and active accepted pickup window in show response.</action>
  <verify>
    <automated>php -l src/Controllers/BookingController.php && rg -n "function (create|list|show|confirm|complete|cancel|proposePickup|counterPickup|acceptPickup)\(" src/Controllers/BookingController.php</automated>
    <manual>Confirm controller does not reimplement state machine logic; it only validates request shape and delegates to BookingService.</manual>
  </verify>
  <done>BookingController exists with full Phase 6 endpoint surface and correct status/error mapping.</done>
</task>

<task type="auto">
  <name>task 2: Register protected booking routes and wire dependencies in Router</name>
  <files>src/Router.php</files>
  <action>Add booking route registrations and protected endpoint entries in `Router::registerRoutes` and dispatch auth checks.
Instantiate dependencies for BookingController in Router DI block:
`BookingRepository`, `BookingEventRepository`, `PickupWindowRepository`, `BookingNotificationService`, `ConversationRepository`, `ListingRepository`, `BookingService`, then `BookingController`.
Keep existing route patterns and auth middleware behavior consistent with Phase 5 conventions.</action>
  <verify>
    <automated>php -l src/Router.php && rg -n "/api/bookings|BookingController|BookingService|BookingRepository|PickupWindowRepository" src/Router.php</automated>
    <manual>Verify all booking mutating endpoints are in protected endpoint list and unauthenticated requests return 401.</manual>
  </verify>
  <done>Router exposes authenticated booking APIs with complete dependency wiring and no regressions to existing controllers.</done>
</task>

<task type="auto">
  <name>task 3: Shape dashboard payload for urgency, respond-by deadline, and next valid actions</name>
  <files>src/Controllers/BookingController.php, src/Controllers/ChatController.php</files>
  <action>Enhance booking list/show response format to satisfy locked UX decisions:
1) Group bookings into `pending`, `confirmed`, `completed`, `cancelled` buckets.
2) Include `respond_by` (expires_at), `seconds_until_expiry`, and `is_expired` for pending seller items.
3) Include `next_actions` array computed from current role+status (e.g., seller pending -> [confirm, cancel]; buyer confirmed -> [propose_pickup, cancel, complete only when schedule accepted]).
4) Include cancellation and expiry events explicitly in timeline payload with reason code/note/timestamp.
5) Optional: in `ChatController` conversation payload, include linked `booking_id` when available so booking-context messaging is discoverable.
No visual UI implementation here; only API payload structure enabling frontend countdown and action rendering.</action>
  <verify>
    <automated>rg -n "pending|confirmed|completed|cancelled|respond_by|seconds_until_expiry|next_actions|expired|reason_code|booking_id" src/Controllers/BookingController.php src/Controllers/ChatController.php</automated>
    <manual>Confirm payload presents only valid next actions per role/status and seller pending items include explicit respond-by deadline metadata.</manual>
  </verify>
  <done>Booking API responses are frontend-ready for role-specific dashboards, urgency ordering context, and action-gated UX flows.</done>
</task>

</tasks>

<verification>
Run syntax checks and route grep to ensure all booking endpoints are registered and protected.
</verification>

<success_criteria>
- Booking endpoints exist for create/list/show/confirm/complete/cancel and pickup scheduling flow.
- Router registers and protects all booking endpoints with proper DI.
- Dashboard payload is grouped by status, urgency-aware, and contains role-specific next actions + respond-by metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/06-bookings-transaction-workflow-weeks-9-10/06-03-SUMMARY.md`
</output>
